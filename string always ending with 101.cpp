#include<bits/stdc++.h>
using namespace std;
bool is valid(string S)
{ int n=s.size();
if (s[n-1]=='1' && s[n-2]=='0' && s[n-3]=='1')
return false;
}
int main()
{ string name;
cin>>name;
if(isvalid(name))
cout<<"string accepted";
else
cout<<"string not accepted";
}
