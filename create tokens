
#include<bits/stdc++.h>
using namespace std;

int nTokens(string s) {
    map<int,int> m;
    for(char x:s){
        m[x-'a']++;
    }
    return m.size();
}

int main(){
    string name;
    cin>>name;
    cout<<nTokens(name);
    return 0;    
}

